baseURL = "https://ptds.samorso.ch/"
languageCode = "en-US"
defaultContentLanguage = "en"

pygmentsCodefences = true
pygmentsCodefencesGuessSyntax = true
pygmentsStyle = "monokai"

title = "Programming tools for data science"
theme = "hugo-theme-learn"
metaDataFormat = "yaml"
defaultContentLanguageInSubdir= true

googleAnalytics = "UA-176152424-1" # add Google Analytics
ignoreFiles = ["\\.Rmd$", "\\.Rmarkdown$", "_files$", "_cache$"]


[params]
  description = "This website provides the support for the class Programming tools in data science given at the Faculty of Business and Economics (HEC Lausanne) of the University of Lausanne in Fall (currently 2023)"
  author = "Samuel Orso"
  # Shows a checkmark for visited pages on the menu
  showVisitedLinks = false
  # Disable search function. It will hide search bar
  disableSearch = false
  # Javascript and CSS cache are automatically busted when new version of site is generated.
  # Set this to true to disable this behavior (some proxies don't handle well this optimization)
  disableAssetsBusting = false
  # Set this to true to disable copy-to-clipboard button for inline code.
  disableInlineCopyToClipBoard = false
  # A title for shortcuts in menu is set by default. Set this to true to disable it.
  disableShortcutsTitle = false
  # When using mulitlingual website, disable the switch language button.
  disableLanguageSwitchingButton = false
  # Order sections in menu by "weight" or "title". Default to "weight"
  ordersectionsby = "weight"
  # Change default color scheme with a variant one. Can be "red", "blue", "green".
  themeVariant = "green"
  # Eneabling Emoji
  enableEmoji=true

[outputs]
home = [ "HTML", "RSS", "JSON"]

[[menu.shortcuts]]
name = "<i class='fab fa-github'></i> Github repo"
identifier = "ds"
url = "https://github.com/ptds2023/"
weight = 5

[[menu.shortcuts]]
name = "<i class='fa fa-fw fa-users'></i> Discussion forum"
identifier = "fm"
url = "https://forms.gle/9EzB7J5mzNkYgCtv7"
weight = 10

[[menu.shortcuts]]
name = "<i class='fa fa-fw fa-book'></i> Lectures"
identifie = "book"
url = "https://smac-group.github.io/ds/"
weight = 15
